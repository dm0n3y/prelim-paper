\begin{figure}
  \vspace{-3px}
  \[
  \arraycolsep=3pt\begin{array}{rlrl}
      \mathsf{LeftTip} & \lTip & ::= & \ltipconv{s} ~\vert~ \ltipconc{s} \\
      \mathsf{RightTip} & \rTip & ::= & \rtipconv{s} ~\vert~ \rtipconc{s}
  \end{array}\]

  \judgbox{
    \matchesSort{\lTip}{\rTip}
  }{
    $\lTip$ expects the same sort as $\rTip$
  }
  \begin{mathpar}
    \inferrule[]{
    }{
      \matchesSort{\ltipconc{s}}{\rtipconv{s}}
    } \ \ \ \ \ \ \ \ \ \
    \inferrule[]{
    }{
      \matchesSort{\ltipconc{s}}{\rtipconc{s}}
    } \\
    \inferrule[]{
    }{
      \matchesSort{\ltipconv{s}}{\rtipconc{s}}
    } \ \ \ \ \ \ \ \ \ \
    \inferrule[]{
    }{
      \matchesSort{\ltipconv{s}}{\rtipconv{s}}
    }
  \end{mathpar}

  \judgbox{
    \fits{\rTip}{\lTip}
  }{
    $\rTip$ fits with $\lTip$
  }
  \begin{mathpar}
    \inferrule[]{
    }{
      \fits{\rtipconc{s}}{\ltipconv{s}}
    } \\
    \inferrule[]{
    }{
      \fits{\rtipconv{s}}{\ltipconc{s}}
    }
  \end{mathpar}

  \caption{
    Syntax of tile and token tips
  }
  \label{fig:tip-syntax}
\end{figure}
